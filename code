print(2)
num = 3
while True:
    correct = 0
    for i in range(2, num):
        if num % i == 0:
            break
        else:
            correct += 1
            if len(range(2, num)) == correct:
                print(num)
    num += 1
